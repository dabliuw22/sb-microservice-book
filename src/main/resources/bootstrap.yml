server:
  port: 8100
spring:
  application:
    name: microservice-book
  jpa:
    show-sql: true
  cloud:
    config:
      uri: http://localhost:8989
eureka:
  client:
    service-url:
      default-zone: ${EUREKA_URI:http://localhost:8761/eureka}